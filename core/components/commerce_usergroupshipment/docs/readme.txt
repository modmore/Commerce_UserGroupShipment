UserGroupShipment for Commerce
------------------------------

Enables a new Shipment type, UserGroupShipment, which will automatically
assign a user to a per-product specific user group.

To use, first add the shipment type to a delivery type, and then select
the usergroup on a product that uses the assigned delivery type.
